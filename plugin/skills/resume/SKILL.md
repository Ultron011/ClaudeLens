---
description: Resume ClaudeLens syncing after /claudelens:pause.
---

Turn syncing back on machine-wide:

```bash
node "${CLAUDE_PLUGIN_ROOT}/dist/claudelens.mjs" resume
```

Relay the output. Tracked projects sync again from the next turn.
